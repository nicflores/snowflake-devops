# Terraform
*.tfstate
*.tfstate.*
.terraform/
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
*.tfvars.local

# Keys — never commit private keys
*.p8
*.pem
snowflake_key*
